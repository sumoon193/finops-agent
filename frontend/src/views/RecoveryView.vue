<script setup lang="ts">import{onMounted,ref}from'vue';import{getRecovery}from'@/api/client';const state=ref<{unknown_queries:number;unknown_tickets:number;requires_reconciliation:boolean}>();onMounted(async()=>{state.value=await getRecovery()})</script>
<template><div class="page"><section class="panel"><header><div><p class="kicker">RECOVERY LEDGER</p><h2>恢复与对账</h2></div><span class="badge" :data-state="state?.requires_reconciliation?'unknown':'completed'">{{state?.requires_reconciliation?'需要对账':'状态一致'}}</span></header><div class="recovery-grid"><article><span>UNKNOWN 查询</span><strong>{{state?.unknown_queries??'—'}}</strong></article><article><span>UNKNOWN 工单</span><strong>{{state?.unknown_tickets??'—'}}</strong></article></div><p class="boundary">UNKNOWN 不等于失败或成功。系统必须查询真实数据库和外部工单系统后才能完成状态收敛。</p></section></div></template>
